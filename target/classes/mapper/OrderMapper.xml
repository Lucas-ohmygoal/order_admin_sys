<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.orderadminsys.mapper.OrderMapper">

    <resultMap id="OrderWithDetailsResultMap" type="com.example.orderadminsys.entity.Order">
        <id column="id" property="id"/>
        <result column="customer_id" property="customerId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="order_date" property="orderDate"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="customer_name" property="customerName"/>
        <result column="product_name" property="productName"/>
    </resultMap>

    <select id="selectAllWithDetails" resultMap="OrderWithDetailsResultMap">
        SELECT o.id, o.customer_id, o.product_id, o.quantity, o.total_price, o.order_date, o.status, o.created_at, o.updated_at,
               c.name AS customer_name, p.name AS product_name
        FROM orders o
        LEFT JOIN customers c ON o.customer_id = c.id
        LEFT JOIN products p ON o.product_id = p.id
        ORDER BY o.created_at DESC
    </select>

</mapper>
